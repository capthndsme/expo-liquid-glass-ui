package expo.modules.liquidglass

/** Shared across the module, the provider registry, and the JS side (`DEFAULT_PROVIDER_ID`). */
internal const val DEFAULT_PROVIDER_ID = "default"

/** Logcat tag for every warning this module emits. */
internal const val LOG_TAG = "ExpoLiquidGlass"
